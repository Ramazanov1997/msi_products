Create table admin(
	id serial primary key

);

Create table users(
	login varchar(255),
	password varchar(255)
	
	
);
CREATE TABLE IF NOT EXISTS products (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    price DECIMAL(10, 2) NOT NULL,
	pic bytea
);

insert into products(name,price,pic)
values
('MSI Immerse GH20',100,'C:/Users/ramy/Desktop/Msi/Headphones/MSI_Immerse_GH20.jpg')
drop table if exists admin
drop table if exists users
drop table if exists products
select * from products
DELETE FROM users;

CREATE TABLE images2 (
    id serial PRIMARY KEY,
    image_url bytea
);
insert into images2(image_url)values('C:/Users/ramy/Desktop/1.webp')


CREATE TABLE images4 (
    id serial PRIMARY KEY,
    image_url bytea
);
insert into images4(image_url)values('C:/Users/ramy/Desktop/1.webp')


select * from images4


CREATE TABLE images3 (
    id serial PRIMARY KEY,
    image_url text
);

INSERT INTO images3 (image_url) VALUES ('C:/Users/ramy/Desktop/1.webp');

select * from images3
